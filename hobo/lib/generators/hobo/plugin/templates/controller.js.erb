import { Controller } from "@hotwired/stimulus"

// The browser half of your tags. `data-controller="<%= plugin_name.tr('_', '-') %>"`
// on an element the tag paints, and this runs.
export default class extends Controller {
  connect() {
    // ...
  }
}
